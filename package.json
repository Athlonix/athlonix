{
  "name": "athlonix-turbo",
  "private": true,
  "scripts": {
    "clean": "turbo clean",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint && biome check --apply .",
    "lint:check": "turbo lint:check && biome ci .",
    "types:check": "turbo types:check",
    "generate:types": "pnpm run -F @repo/types run generate:types",
    "pre-commit": "pnpm run lint:check && pnpm run types:check"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.4",
    "@repo/biome-config": "workspace:*",
    "@repo/supabase": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "turbo": "^1.13.2"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=20"
  }
}
